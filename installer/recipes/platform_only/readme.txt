platform_only

 Platform Setup:
   1. Setup platform using command ./install.sh -r platform_only setup -A initAdminPassword=[password]
   2. Initialize platform using command ./install.sh -r platform_only initialize -A initAdminPassword=[password]
   3. Start platform using command ./install.sh -r platform_only start
